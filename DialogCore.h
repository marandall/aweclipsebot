#pragma once

/* helper functions to access the dialog core */
bool IsRootMenuChecked		(int MenuItem);
void SetRootMenuChecked		(int MenuItem, bool Checked);
void SetRootMenuEnabled		(int MenuItem, bool Enabled);